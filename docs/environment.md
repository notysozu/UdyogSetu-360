# Environment

Use the root `.env.example` as the canonical reference for local and deployment configuration.

Key groups:

- Core app ports for gateway, portals, and services
- MongoDB, Kafka, RabbitMQ, and n8n integration settings
- Security secrets for session and JWT handling
- Feature flags for mock adapters and AI
