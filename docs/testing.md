# Testing

Workspace tests currently focus on lightweight verification:

- universal case ID format
- event envelope creation
- orchestration transition guards
- adapter registry lookup
- gateway app construction

The legacy `web/` tests continue to cover homepage and cookie-consent behaviour.
