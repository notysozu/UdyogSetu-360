<section class="card">
  <h2>Something went wrong</h2>
  <p>Please try again. If the problem continues, share the correlation ID with support.</p>
  <% if (correlationId) { %><p><code><%= correlationId %></code></p><% } %>
</section>
