<% if (success && success.length) { %>
  <div class="alert alert-success" role="status" aria-live="polite"><%= success[0] %></div>
<% } %>
<% if (error && error.length) { %>
  <div class="alert alert-error" role="alert" aria-live="assertive"><%= error[0] %></div>
<% } %>
