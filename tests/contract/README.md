# Contract Tests

Contract boundaries are described in `docs/contract-test-plan.md`.

Run starter checks:
```bash
node --test tests/contract/*.test.js
```

The initial files validate local schema fixtures and envelope conventions. Connect them to live services once staging endpoints are stable.
