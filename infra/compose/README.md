# Compose Profiles

- `docker-compose.yml`: main local development stack.
- `docker-compose.dev.yml`: development overrides.
- `docker-compose.test.yml`: test-time service overrides.
- `docker-compose.observability.yml`: Prometheus/Grafana placeholders.
