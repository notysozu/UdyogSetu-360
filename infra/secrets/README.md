# Secrets

This folder contains templates only. Do not place real secrets in this repository.

Use a production secret manager or Kubernetes external secrets for:
- JWT secrets
- MongoDB URI
- Kafka credentials
- RabbitMQ credentials
- S3 credentials
- n8n encryption key
- AI service keys
- OIDC secrets
- SMTP/SMS credentials
- DigiLocker credentials
