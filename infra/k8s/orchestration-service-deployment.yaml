apiVersion: apps/v1
kind: Deployment
metadata:
  name: orchestration-service
  namespace: udyogsetu
spec:
  replicas: 2
  selector:
    matchLabels:
      app: orchestration-service
  template:
    metadata:
      labels:
        app: orchestration-service
    spec:
      containers:
        - name: orchestration-service
          image: registry.example/udyogsetu/orchestration-service:latest
          ports:
            - containerPort: 4101
          envFrom:
            - configMapRef:
                name: udyogsetu-config
            - secretRef:
                name: udyogsetu-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 4101
